package tv.twitch.android.shared.chat;

import tv.twitch.chat.library.ChannelEvent;
import tv.twitch.chat.library.ChatEvent;

public class ChatController2 {
    /* ... */


    private final ChatChannelEvent toEvent(ChatEvent chatEvent) {
//        ChannelEvent event = chatEvent.getEvent();
//        if (event instanceof ChannelEvent.ChatChannelUserMessagesCleared) { // TODO: __REPLACE_CODE
//            return new ChatChannelEvent.ChannelUserMessagesCleared(
//                    chatEvent.getChannelId(), ((ChannelEvent.ChatChannelUserMessagesCleared) event).getClearUserId(),
//                    ((ChannelEvent.ChatChannelUserMessagesCleared) event).getComponents()
//            );
//        }

        return null;
    }

    /* ... */
}
